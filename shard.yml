name: loci
version: 0.2.0
description: Unified code intelligence interface — ctags + compiler-backed analysis

authors:
  - Thomas Sawyer <transfire@gmail.com>

dependencies:
  ignore:
    github: trans/ignore

targets:
  loci:
    main: bin/loci.cr

crystal: '>= 1.18.2'

license: MIT
